case "$1" in
  -h)
    ./bin/lastcov.cobertura -h;;
esac
ls projects/*/target/site/cobertura/frame-summary.html | awk -F\/ '{print $2}' | while read a; do (cd projects/$a && ../../bin/lastcov.cobertura $a ); done
